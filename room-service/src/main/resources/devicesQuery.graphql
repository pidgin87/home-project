query GetDeviceListByRooms {
    getDevicesByRooms(roomIds: $roomIds) {
        id
        name
        roomId
    }
}

query GetStatusesByDevices($deviceIds: [String!]) {
    getStatusesByDevices(deviceIds :$deviceIds) {
        deviceId
        code
        value
    }
}